#include <bits/stdc++.h>
using namespace std;

int main() {
    long long int n, m, k;
    cin >> n >> m >> k;
    vector <long long int> apps, peeps;
    
    for(int i = 0; i < n; i++){
        cin >> apps[i];
    }
    for(int j = 0; j < m; j++){
        cin >> peeps[j];
    }
}